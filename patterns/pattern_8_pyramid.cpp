//remaining to do 
#include<iostream>
using namespace std;
int main() {
	int row = 1, tot = 5;
	while(row <= tot) {
		col = 1;
		while(col <= row) {
			k = 1;
			while(k <= row) {
				k = tot - k + 1;
			
			}
		}
	}
}
